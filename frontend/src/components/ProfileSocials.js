import React from "react";

// Follower / Following Components (displays all users that are following /
// followed by the current user (hint: profile preview components - when looking at a profile then display the profile overlay)
// - this can be one component that handles the display of both, or a separate component for each)

export class ProfileSocials extends React.Component {
  render() {
    return <></>;
  }
}
