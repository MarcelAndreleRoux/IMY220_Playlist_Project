import React from "react";

// displays the song / playlist preview components - this can be one component that handles the
// display of both feeds, or a separate component for each

export class Feed extends React.Component {
  render() {
    return <></>;
  }
}
